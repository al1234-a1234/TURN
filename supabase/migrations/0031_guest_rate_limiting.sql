-- حدّ المعدّل داخل القاعدة لدوال الضيف (مطبَّق على الإنتاج — النص الكامل في
-- سجلّ ترحيلات Supabase بنفس الاسم).
--
-- المفتاحان: الرقم (إساءة الفرد) والفرع (إغراق الأرقام العشوائية) — لا IP لأن
-- كل الطلبات تمرّ من خادم Next بعناوين Vercel نفسها فتُخنق معًا.
-- join_waitlist_guest: 3/10د لكل رقم + 30/د لكل فرع (بعد فحص الدور القائم
-- كي لا يُحاسَب تحديث التذكرة). submit_review: 5/يوم لكل رقم.
-- check_rate/rate_limits بلا منح لأي دور: تُستدعى فقط من دوال DEFINER —
-- فلا يحرق أحد ميزانية غيره باستدعاء مباشر. اختُبرت حيًّا: 3 تمرّ والرابعة تُرفض.
