# BRIEF — مشروع «دور / Turn» (نسخة مُفصّلة)

> ملخّص جاهز للّصق في محادثة مع مساعد لا يعرف المشروع. الهدف: أن يفهم المشروع كاملًا **ويفهم المقصد من كل فكرة**.
> كل ما هنا مأخوذ من الكود ومن قاعدة الإنتاج الحيّة (Supabase). عند الشك مكتوب «غير واضح». مع كل مفهوم مهم سطر **«المقصد»** يشرح لماذا صُمّم هكذا.

---

## ١. ما هو التطبيق؟ (والمقاصد الكبرى)

«دور / Turn» منصّة **قوائم انتظار + حجوزات + تسويق للمطاعم** في السعودية، عربية بالكامل RTL (مع تبديل إنجليزي). 

**المشكلة التي يحلّها:** المطعم يعاني من فوضى الطابور على الباب، ومن أنه **لا يملك عملاءه** — الزبون يأكل ويطير ولا وسيلة لإرجاعه، والإعلان على إنستقرام/سناب يكلّف ولا يصل. التطبيق يحوّل كل زائر إلى **جهة اتصال محفوظة** يُسوّق لها، ويظهر المطعم أمام عملاء المنصّة كلها.

**النموذج المحوري «الضيف أولًا»:** العميل يتعامل بلا حساب — **اسم + رقم جوّال فقط** — وبياناته تُحفظ محليًّا على جهازه (localStorage) وتُربط برقمه في القاعدة. لا يوجد تسجيل دخول للعميل إطلاقًا.
- **المقصد:** أقل احتكاك ممكن. الزبون الجائع عند الباب لن يسجّل حسابًا ويؤكّد بريدًا؛ رقمه يكفي. هذا يرفع نسبة من «يأخذ دوره» فعلًا، ويجعل التطبيق يعمل على الويب بلا تحميل.

**فئات المستخدمين الثلاث (فعليًّا في الكود):**
- **العميل** (ضيف عام): يكتشف، يأخذ دوره، يمسح باركود المطعم لهدية ترحيب، ويرى هداياه/عروضه/نقاطه برقمه.
- **الطاقم/المالك** (موظفو المطعم): يدخلون بوّابة الشركاء بحساب، ويديرون كل شيء حسب صلاحياتهم.
- **مشرف المنصّة**: يرى كل المطاعم، ينشئها، يتحكّم بالموديولات، ويفتح لوحة أي مطعم كأنه مالكه.

**فكرة «الموديولات» (Feature Modules):** كل ميزة (طابور، حجوزات، عروض، ولاء، CRM، تقييمات، «امسح خذ هديتك»…) موديول مستقل يُفعَّل/يُطفأ لكل مطعم على حدة، والمشرف يتحكّم بالباقات.
- **المقصد:** تفصيل المنصّة حسب نوع المطعم (مطعم مزدحم يحتاج الطابور، سفري يحتاج «امسح خذ هديتك»)، وفتح باب التسعير بالباقات، وإبقاء لوحة كل مطعم نظيفة بما يخصّه فقط.

**الميزة الأحدث «امسح خذ هديتك»:** باركود على طاولة/كاشير المطعم؛ العميل يمسح → يكتب رقمه → **يستلم هدية ترحيب فورية** → تُسجّل زيارته وتُبنى للمطعم قاعدة عملاء.
- **المقصد:** حلّ عقدة «المطعم اللي ما يحتاج طابور» (سفري/محلّ صغير). بلا طابور لا توجد بيانات؛ فجعلنا **الهدية الفورية** هي الدافع الأناني الذي يجعل العميل يسجّل رقمه بنفسه، والمطعم يكسب قاعدة عملائه كأثر جانبي — بدون تطبيق وبدون دفع وبدون أي عمل على المطعم غير لصق ورقة.

**الرؤية العامة:** أن تصبح «دور» **شبكة عملاء** تجمع المطاعم التي تحتاج تنظيم طابور والمطاعم العادية معًا، فكل مطعم ينضم يزيد قيمة المنصّة لبقية المطاعم (اكتشاف + عملاء جدد).

---

## ٢. رحلات المستخدمين خطوة بخطوة (مع المقصد)

### العميل (ضيف)
1. يفتح `/` → اكتشاف المطاعم: شريط «عروض حيّة»، زر «تصفية» بالمطابخ، بطاقات مجمّعة (**متاح الآن · فيه طابور · لا يستقبل**) مع المسافة من موقعه. **المقصد:** يقرّر بلمحة وين يروح.
2. يفتح مطعمًا `/r/[slug]` → الصورة/المنيو/التقييمات/العروض + حالة الطابور (داخلي/خارجي).
3. يأخذ دوره: اسم + رقم → ينضمّ للطابور، ويرى رقم دوره وحلقة تقدّم. **المقصد:** ينتظر وهو مرتاح بدل الوقوف على الباب.
4. بديلًا: يمسح باركود `/g/[slug]` → رقمه → **هدية ترحيب** + زيارة مُسجّلة + نقاط ولاء. **المقصد:** يربط العميل بالمطعم حتى بلا طابور.
5. «أخرى» `/me` → هداياه/خصوماته `/me/rewards` (بحث برقمه)، اشتراكاته، المفضّلة، يومياته.
6. يقدّم الهدية عند الطلب؛ الطاقم يعتمدها من لوحته. **المقصد (اعتماد من الطاقم لا العميل):** منع «حرق» المكافأة ذاتيًّا.

### موظف المطعم (طاقم/استقبال)
1. يدخل `/partners`: **مُعرّف المطعم + اسم المستخدم + رمز**. يُتحقّق أنه ضمن طاقم هذا المطعم فعلًا. **المقصد:** ربط الدخول بالمطعم الصحيح ومنع تسرّب موظف لمطعم غيره.
2. لوحة `/dashboard` تعرض **فقط** ما تسمح به صلاحياته (٩ صلاحيات مفصّلة).
3. الاستقبال `/dashboard/reception`: الطابور الحيّ، إضافة walk-in، تنبيه/جلوس/إزالة، فتح ملف العميل.
4. يعتمد المكافآت، يدير الحجوزات، ينشر/يخفي التقييمات، يحدّث القائمة/الطاولات حسب صلاحيته.
> ملاحظة تقنية: «الحيّ» هنا يعتمد إعادة تحميل الصفحة، لا Realtime.

### صاحب المطعم (owner/manager)
1. نفس دخول `/partners` (دوره owner/manager) → يملك **كل** الصلاحيات ضمنيًّا. **المقصد:** المالك لا يُقيَّد بصلاحيات؛ التقييد للموظفين فقط.
2. لوحة كاملة: نظرة عامّة، الاستقبال، الحجوزات، «امسح خذ هديتك» (ضبط الهدية + باركود للطباعة + إحصاءات)، العروض، الولاء، **العملاء (CRM كامل:** زيارات وتواريخها وأوقات الجلوس، منح هدية/خصم، حملات لشرائح VIP/فضّي…)، التقييمات، التقارير، الفريق والصلاحيات، الطاولات، المحتوى والروابط، إعدادات المطعم والقائمة.
3. يمنح موظفيه صلاحيات مفصّلة، ويضبط برنامج الولاء وهدية الترحيب.
- **المقصد:** المالك أداة إدارة + معلومات + **أداة تسويق وجذب** في آنٍ واحد.

### مدير المنصّة (admin)
1. `/admin` (يُتحقّق `is_platform_admin`) → كل المطاعم + إنشاء مطعم (عبر edge function تُنشئ الحساب وتعيد كلمة المرور مرّة واحدة).
2. `/admin/[id]` → تفعيل/تعطيل موديولات مطعم (الباقات).
3. زر «افتح اللوحة» → يضع كوكي `admin_rid` (بعد التحقّق أنه مشرف) فيتصرّف كمالك أي مطعم ويرى لوحته كاملة، ويخرج بزر «خروج».
- **المقصد:** دعم/إشراف كامل — المشرف يقدر يدخل أي مطعم لحلّ مشاكله أو معاينته، دون الحاجة لرموز المطعم.

---

## ٣. الجداول الـ٢٥ (دور كل واحد + العلاقات)

1. **restaurants** — المطعم (اسم، slug، شعار، مطبخ، مالك، رمز تسليم، تفعيل).
2. **branches** — فروع المطعم (مدينة، إحداثيات، تفعيل).
3. **branch_settings** — إعدادات كل فرع (يستقبل طابور/حجوزات، أوقات، حدّ المجموعة).
4. **tables** — طاولات الفرع (مقاعد، منطقة، حالة).
5. **customers** — العملاء (اسم، جوّال، user_id اختياري) — **سجلّ عالمي مشترك عبر كل المطاعم**. المقصد: العميل واحد عبر المنصّة (أساس الشبكة).
6. **customer_restaurant** — علاقة عميل↔مطعم (زيارات، نقاط، شريحة، VIP، حظر، أول/آخر زيارة). المقصد: ملف العميل **خاص بكل مطعم** فوق السجلّ العالمي.
7. **customer_rewards** — هدايا/خصومات العميل (نوع، قيمة، كود، حالة، انتهاء، اعتماد).
8. **checkins** — سجلّ زيارات «امسح خذ هديتك» (خفيف، غير الطابور). المقصد: التقاط الزيارة بلا طابور.
9. **checkin_settings** — إعداد هدية الترحيب لكل مطعم.
10. **loyalty_programs** — برنامج الولاء (نقاط/زيارة، حدّ المكافأة).
11. **waitlist_entries** — صفوف الطابور (منطقة، ترتيب، حالة، أوقات).
12. **reservations** — الحجوزات (وقت، مدّة، مدى زمني `tstzrange`، طاولة، حالة).
13. **offers** — عروض عامّة (نوع، قيمة، جمهور all/new، مدّة، كود).
14. **offer_redemptions** — سجلّ استخدام العروض.
15. **reviews** — التقييمات (نجوم، تعليق، منشور؟، وُجّه لقوقل؟).
16. **menu_categories** — أقسام القائمة.
17. **menu_items** — أصناف القائمة (سعر، صورة، متوفّر؟).
18. **restaurant_photos** — معرض صور المطعم.
19. **staff** — الطاقم (دور، صلاحيات jsonb، فرع، تفعيل).
20. **feature_modules** — كتالوج الموديولات المتاحة بالمنصّة.
21. **restaurant_features** — تفعيل الموديولات لكل مطعم (تحكّم المشرف).
22. **daily_stats** — إحصاءات يومية مجمّعة لكل فرع.
23. **owner_insights** — رؤى/تنبيهات للمالك.
24. **notifications** — جدول إشعارات (قنوات sms/whatsapp/push/email) — **غير مستخدم في الكود** (بنية جاهزة فقط).
25. **platform_admins** — مشرفو المنصّة.

**العلاقات:** `restaurant → branches → (branch_settings 1:1, tables, waitlist_entries, reservations, daily_stats)`. `restaurant → (staff, offers, reviews, menu_categories → menu_items, restaurant_photos, loyalty_programs 1:1, checkin_settings 1:1, restaurant_features → feature_modules, owner_insights)`. `customer` يرتبط بالمطاعم عبر `customer_restaurant`، ويملك `customer_rewards / waitlist_entries / reservations / checkins / offer_redemptions / reviews`. (جدول `subscriptions` أُنشئ قديمًا ثم حُذف.)

---

## ٤. المسارات (من يصل + ماذا تفعل)

**عامّة (بلا مصادقة):** `/` الاكتشاف · `/r/[slug]` المطعم + الطابور · `/g/[slug]` «امسح خذ هديتك» · `/search` بحث · `/about` · `/contact` · `/diaries` · `/me` حساب الضيف · `/me/rewards` هداياه بالرقم · `/me/favorites` · `/me/waitlist` · `/restaurants`→`/` · `/login`→`/partners` · `/partners` بوّابة الشركاء.

**لوحة المطعم `/dashboard/*` (موظف فعّال + صلاحية + موديول):** `/dashboard` نظرة عامّة · `reception` الاستقبال (waitlist) · `reservations` (reservations) · `checkin` الباركود (checkin+loyalty) · `offers` (offers) · `loyalty` (loyalty) · `customers`(+`/[id]`) CRM (crm+customers) · `reviews` (reviews) · `reports` (analytics) · `staff` الفريق (team) · `tables` (settings) · `content` (settings) · `manage` الإعدادات والقائمة (settings).

**مشرف المنصّة:** `/admin` كل المطاعم + إنشاء · `/admin/[id]` موديولات مطعم. (يتطلّبان `is_platform_admin`.)

**الحماية:** middleware يعمل فقط على `/dashboard/*` و`/admin/*` (يحوّل غير المسجّل لـ`/partners`)؛ باقي المسارات عامّة. كل صفحة لوحة تعيد فحص الصلاحية داخليًّا.
- **المقصد (طبقتان):** صفحات العميل العامّة لا تمرّ بالـmiddleware إطلاقًا = **سرعة قصوى وصفر اتصال مصادقة**، والحماية مركّزة على اللوحة/الأدمن فقط.

---

## ٥. الدوال (٣٤) + edge + cron (اسم ووظيفة)

**دوال القاعدة (كلها SECURITY DEFINER إلا المُشار لها INVOKER):**
- `join_waitlist_guest` — انضمام ضيف للطابور بالاسم+الرقم.
- `cancel_waitlist_guest` — إلغاء ضيف لدوره (بالرقم).
- `create_reservation_guest` — إنشاء حجز ضيف.
- `public_checkin` — «امسح خذ هديتك»: زيارة + هدية ترحيب أول مرّة + نقاط ولاء (مع منع تكرار كل ٤ ساعات).
- `redeem_customer_reward` — اعتماد مكافأة عميل.
- `get_customer_rewards` — هدايا العميل بالرقم.
- `get_customer_loyalty` — نقاط/ولاء العميل بالرقم.
- `grant_reward_to_segment` — منح مكافأة لشريحة عملاء (حملة set-based).
- `waitlist_counts` — عدّاد طابور فرع واحد.
- `waitlist_counts_for` — عدّاد طوابير عدّة فروع (محصور بفروع الصفحة).
- `active_waitlist_counts` — عدّاد الطوابير النشطة (عام).
- `admin_create_restaurant` — إنشاء مطعم بواسطة المشرف.
- `create_restaurant_with_branch` — إنشاء مطعم+فرع (تزويد ذاتي).
- `claim_restaurant` — المطالبة بمطعم برمز — **غير مستدعاة من الكود**.
- `gen_claim_code` (INVOKER) — توليد رمز تسليم عشوائي.
- `set_staff_permission` — ضبط صلاحية موظف.
- `has_feature` — هل موديول مفعّل لمطعم.
- `is_staff_of` / `is_manager_of` / `is_platform_admin` / `staff_has_perm` / `staff_can_read_customer` / `restaurant_of_branch` — **مساعدات RLS** (أساس العزل بين المطاعم).
- `set_reservation_time_range` (INVOKER) — trigger يملأ `time_range` للحجز (قبل الإدراج، لتفادي خطأ عدم الثبات 42P17).
- `set_waitlist_position` — trigger يحسب ترتيب الطابور.
- `on_waitlist_status_change` — trigger مكافأة/نقاط ولاء عند الجلوس.
- `create_default_branch_settings` — trigger إعدادات افتراضية لفرع جديد.
- `touch_updated_at` (INVOKER) — trigger تحديث الطابع الزمني.
- `rls_auto_enable` — أداة تفعيل RLS تلقائيًّا.
- `rollup_daily_stats` / `rollup_all_daily_stats` — تجميع إحصاءات فرع/كل الفروع ليوم.
- `run_daily_digest` — ملخّص يومي. `run_slow_hours` — منطق الأوقات البطيئة.
- `demo_live_activity` — **مولّد نشاط تجريبي وهمي** (غير مُعرّف بالمستودع، مجدول كل دقيقة).

**Edge function:** `provision-owner` — تُنشئ حساب مالك/مطعم وتعيد كلمة المرور؛ تُستدعى من الأدمن، **كودها غير موجود في المستودع**.
- **المقصد من نمط DEFINER:** العميل (anon) لا يملك صلاحية كتابة مباشرة في الجداول؛ كل كتابات الضيف (طابور/حجز/تسجيل حضور/مكافأة) تمرّ عبر دالة آمنة تتحقّق وتكتب نيابةً عنه. هذا يمنع التلاعب المباشر مع إبقاء التجربة بلا حساب.

**مهام cron (نشطة على الإنتاج):** 00:10 `rollup_all_daily_stats(أمس)` · 00:20 `run_daily_digest` · كل ساعة `run_slow_hours` · **كل دقيقة `demo_live_activity`** ⚠️ (تجريبي — يجب إيقافه).

---

## ٦. التقنيات والإصدارات

- **Next.js** `^15.5.20` (App Router) · **React/React-DOM** `19.2.4` · **TypeScript** `^5`.
- **Tailwind CSS v4** (إعداد عبر CSS في `globals.css`، بلا `tailwind.config`).
- **Supabase**: `@supabase/ssr ^0.12.3` + `@supabase/supabase-js ^2.110.7` (Postgres + Auth بالكوكيز + Storage + RLS).
- **qrcode** `^1.5.4` (باركود الملصق). مدير الحزم **npm**. نشر **Vercel** (`fra1`) + Supabase (`eu-central-1`).
- المنطق الخلفي كلّه **Server Actions** (لا API routes) + دوال قاعدة البيانات.
- **المقصد المعماري:** الاعتماد على RLS + دوال DEFINER يجعل القاعدة هي **حارس الأمان الأساسي**، والتطبيق طبقة عرض/تنسيق فوقها.
- **تنبيه:** `next.config.ts` يتجاهل أخطاء TypeScript وESLint أثناء البناء. لا اختبارات ولا CI.

---

## ٧. ما يعمل فعليًا اليوم

- طابور الضيف كامل (انضمام/إلغاء + إدارة: تنبيه/جلوس/إزالة) + walk-in من الاستقبال.
- «امسح خذ هديتك» (`public_checkin`): زيارة + هدية ترحيب + نقاط ولاء — **مُختبر فعليًّا**.
- عرض هدايا/عروض/نقاط العميل برقمه.
- منح/إلغاء/اعتماد المكافآت + حملات الشرائح من اللوحة.
- العروض: إنشاء/تعديل + عرضها للعميل + شريط «عروض حيّة» بالرئيسية.
- الولاء + إعداد هدية الترحيب.
- إدارة المطعم/القائمة/الطاولات/المحتوى + رفع الصور إلى Supabase Storage.
- الحجوزات من **جهة الموظف**. الطاقم والصلاحيات المرنة.
- توفير المطاعم بواسطة المشرف + تبديل الموديولات + «عرض المشرف» عبر `admin_rid` (مبوّب بفحص is_platform_admin).
- التقارير من بيانات فعلية · دخول الشركاء · صفحات العميل المحلّية · تذكير واتساب يدوي.
- **الأمان:** RLS مفعّل على كل الجداول الـ٢٥؛ العزل عبر دوال DEFINER؛ كتابات الضيف عبر دوال آمنة فقط.

---

## ٨. ما هو ناقص أو معطّل أو وهمي

- **استقبال التقييمات غير منفّذ**: لا إدراج في `reviews` ولا واجهة إدخال للعميل — اللوحة تنشر فقط.
- **«توجيه التقييم الذكي» تجميلي**: لافتة تعدّ `routed_to_google` بلا أي كتابة لهذه القيمة.
- **حجوزات العميل معلَنة بلا مسار**: الحجز من اللوحة فقط، لا واجهة حجز للعميل.
- **«إرسال الحملة» لا يُبلّغ أحدًا**: يُدرج مكافآت فقط؛ لا قناة SMS/Push/Email في المستودع.
- **الإشعارات**: جدول `notifications` جاهز لكنه غير مستخدم، ولا إرسال فعلي لأي قناة.
- **Realtime غير مستخدم إطلاقًا**: الشاشات تعتمد `revalidatePath`.
- **`demo_live_activity`** مولّد وهمي مجدول كل دقيقة على الإنتاج، محتواه **غير واضح**.
- **`claim_restaurant`** دالة يتيمة غير مستدعاة.
- **انجراف الهجرات**: ~١٣ جدول و٨ دوال + edge `provision-owner` موجودة على الإنتاج لكن **غير موجودة في ملفات الهجرة** → لا يمكن إعادة بناء القاعدة من المستودع.
- **لا اختبارات، لا CI**، والبناء يتجاهل أخطاء الأنواع/ESLint.

---

## ٩. أخطر عشر نقاط لو أُطلق على ألف مطعم (مرتّبة + سبب كل واحدة)

1. **انجراف الهجرات** — المستودع لا يعيد بناء قاعدة الإنتاج (جداول/دوال/edge مفقودة) → لا استرداد ولا بيئة اختبار.
2. **تسريب بيانات العميل بالرقم** — `get_customer_rewards/get_customer_loyalty` عامّة لـanon بلا تحقّق؛ أي رقم يكشف هدايا/نقاط صاحبه.
3. **لا Rate Limiting على دوال anon** — إغراق الطوابير/الحجوزات/التسجيلات بصفوف وهمية وتلويث البيانات وتضخّم الكلفة.
4. **cron تجريبي كل دقيقة على الإنتاج** — `demo_live_activity` يفسد التحليلات والطوابير الحقيقية ويستهلك الموارد.
5. **البناء يتجاهل أخطاء الأنواع/ESLint** — أخطاء تُنشر صامتة بلا اختبارات/CI.
6. **لا Realtime + revalidatePath ثقيل** — شاشات الطابور/الاستقبال لا تتحدّث تلقائيًّا، وحمل أعلى مع الحجم.
7. **هويّة العميل = رقم غير موثّق + localStorage** — اختطاف محفظة أي شخص بمعرفة رقمه (لا OTP).
8. **ميزات مُعلَنة غير عاملة** — التقييمات/التوجيه الذكي/حجوزات العميل/«إرسال» الحملة → فجوة مصداقية.
9. **جاهزية الحِمل/النسخ الاحتياطي غير مؤكّدة** — لا دليل على PITR/ترقية حوسبة؛ قائمة الاكتشاف بلا فهرس على `is_active/created_at`؛ لا صفحنة عميقة.
10. **نظافة الأسرار** — `.env.production` متتبَّع في git (مفتاح عام الآن، نمط خطر)، ومشرف/بريد اصطناعي ثابتان، وكلمات مرور مطاعم بلا سياسة تدوير.

---

*انتهى. لم يُنشأ/يُعدّل أي ملف غير BRIEF.md.*
